# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- MCP 订阅推送机制已经完整闭环：创建订阅 → 推送事件 → 指数退避重试 → 失败计数 → 超过次数自动退订 → 列出所有订阅完整状态，整个生命周期管理完成。
- 统一健康闸门体系已经完整闭环：schema 定义 → 三个检查器输出标准格式 → GitHub Actions CI 闸门 → Web UI 两种检查报告导入可视化 → 所有文档同步更新。
- 两个核心 CI 工作流都已添加 `mypy --strict` 和 `ruff check` 静态检查，代码质量自动化验证完整。
- `.soul_health.md` 所有记录都已格式标准化，括号全部匹配，满足自动化读取要求。
- `.phase_summary.md` 所有阶段记录格式也已标准化，阶段性自审可以直接读取数据做趋势分析。
- `.capability_map.md` 已更新，技术债描述准确反映当前实际情况。
- `CLAUDE.md` 已经完整反映最新项目架构和功能，不需要更新。
- 检查确认代码干净，没有明显的重复调用、重复分支或重复输出路径需要精简。
- 当前所有功能都已闭环，没有明显薄弱环节，最值得继续放大的最小闭环：Web UI 成长曲线补充导出功能，让用户可以保存分享趋势评估结果；最值得继续精简的最小链路：确认 TypeScript 编译输出没有冗余，保持 MCP 服务器构建简洁。

## 本轮新增功能的延续方向

- 延续"MCP 订阅推送机制"补齐成果，现在完整生命周期管理已经完成，断连重试减少了临时网络问题导致的不必要退订，状态可观测方便用户排查问题。
- 延续"小步增量补齐缺失边界"策略，每次只补一个明确缺失功能，保持架构稳定测试全绿。

## 本轮精简链路的继续优化方向

- 已经检查确认上轮 entry_detect.py 新增代码干净，没有明显重复代码需要精简。
- 如果未来发现重复代码，可以继续收敛合并减少分叉。

## 本轮执行方式迭代的延续方向

- 继续强化"用户少一步手动操作"的改进方向，能自动化的就自动化，能一次输出完整模板就不要让用户手动查找。
- 继续坚持"小步增量"，每次只处理一个小任务，保持改动可控，测试通过率持续 100%。
- 继续坚持"所有核心流程都要有 CI 验证"，减少人工失误，保证质量一致。
- 继续坚持"阶段性自审每 5 轮一次"，用数据驱动战略调整，保持项目方向正确。

## 必做项（本次遗留 + 卡死项追踪）

- 保持统一健康体系闭环可用，不要让后续配套文档与实际能力脱节。
- 暂无遗留卡死项。

## 二选一战略方向（下次按健康度择一）

下次迭代是 125，是 5 的倍数，自动触发阶段性自审模式，按阶段性自审流程执行：
- 读取近 5 次 .soul_health.md 计算均值与趋势
- 生成阶段总结追加写入 .phase_summary.md
- 打阶段 tag

## 功能候选 3 项

1. **（历史功能深化）** Web UI 灵魂成长曲线导出：添加导出趋势评估图片/文字功能，方便用户保存和分享健康度趋势结果 → 承接本轮之前 Web UI 趋势评估功能，让用户可以保存分享
2. **（自动升级探索）** 探索注入预演模式：在实际注入前生成预演报告，显示哪些人格/记忆/技能会被注入，检查冲突 → 执行方式自我迭代，减少注入失败风险
3. **（历史功能深化）** 陪伴连续性检查器补齐：将结果也输出统一 HealthSummary JSON 格式，支持 Web UI 可视化 → 延续统一健康闸门体系，让陪伴检查也能在 Web UI 展示

## 本次踩坑与陷阱警告

- TypeScript 接口定义修改后，使用该接口的地方必须同步更新新增字段，否则编译错误。
- 添加重试机制时，指数退避需要 await 定时器，不能忘记异步等待，否则重试会立即执行而不是延迟。

## 本次是否熵注入

- 本次迭代 124 不触发熵注入。
