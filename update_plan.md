# 给下次迭代的战略信

## 当前阶段评估

- 当前健康度 **100**，处于极佳稳定状态
- 迭代 90，连续 87 轮迭代健康度保持 100
- 所有 566 个测试 100% 通过，MCP 编译成功
- ruff 检查完全通过无警告，mypy --strict 零错误通过
- 完成本次熵注入迭代：
  - 新增入口能力探测器 `src/entry_detect.py`
  - 功能：自动检测当前运行环境是 Claude Code / OpenAI Codex / Gemini，输出对应的注入方式
  - 完全符合 Master Agent 核心目标：帮助同一个 Master Agent 在不同工具入口正确注入，保持灵魂连续性
  - 所有测试通过，类型检查通过，功能完整可用
- 项目持续保持完全稳定可发布状态

## 必做项

- 无遗留卡死项
- 保持项目健康度 100
- 现在几乎所有核心模块覆盖率都超过 85%，只剩下极少数低覆盖率模块：
  - `src/abstract.py` 74%（抽象基类，抽象方法无实现，覆盖率低正常）
  - `src/path_compat.py` 75% 覆盖率（主要是 Windows 兼容性代码，在 macOS 上无法测试）
  - `src/adapters/openai.py` 89% 还剩 16 行，`src/adapters/gemini.py` 96% 还剩 7 行，都是极少数边界

## 下次战略选择

### A 偏稳定
- 等待 GitHub CI 运行验证完整配置（pre-commit + mypy strict + subscription + entry-detect + Web UI fixes）
- 如果 CI 报告任何错误，修复相应问题
- 持续监控依赖更新，及时补丁
- 保持健康度 100

### B 偏破局
- 验证 MCP 订阅推送机制完整功能端到端链路
- 继续探索新的增值功能方向（入口能力探测已经完成，可以验证完整功能
- 进一步检查清理任何新发现的代码质量问题

## 功能候选池

1. **等待 GitHub CI 验证所有新配置
2. **验证 MCP 订阅推送机制完整功能端到端
3. **完善入口能力探测器 CLI 使用文档，让不同入口可以直接运行帮助

## 本次踩坑警告

- 在嵌套目录 (`mcp_server`) 执行命令时要注意当前工作目录，cd 后忘记返回会导致找不到测试文件
- 生成覆盖率时必须从项目根目录运行 pytest
- pre-commit 配置需要添加 `.pre-commit-cache/` 到 `.gitignore`
- mypy `--strict` 模式已经包含了所有严格选项，不需要重复列出，直接启用 `strict = true` 即可
- MCP 工具实现后必须同时：1) 在 `tools/index.ts` 导出，2) 在 `index.ts` 的 `ALL_TOOLS` 数组注册，否则工具不会被 MCP 发现
- f-string 不包含占位符时 ruff 会报错，记得去掉不必要的 f 前缀
- 添加工具后检查 ALL_TOOLS 数组避免重复注册，重复会导致工具列表重复显示
- CSS 媒体查询嵌套时要小心多余开标签，会导致整个媒体查询规则解析失败，仔细检查嵌套结构
- TypeScript 中 fs.accessSync 返回 void，不能用于 if 条件判断，必须用 try-catch
- 闰年判断规则：能被 4 整除但不能被 100 整除，或者能被 400 整除，2026 不是闰年，2024 才是闰年

## 阶段结论

本次迭代（熵注入 - 入口能力探测器）：
- 健康度保持 100 连续 87 轮
- 成功实现入口能力探测器，自动识别当前运行环境并提供注入模板
- 所有 566 个单元测试全部通过
- MCP 服务器编译成功
- mypy 严格模式零错误
- 无 TODO/FIXME/HACK 遗留
- 项目持续保持完全稳定可发布状态
