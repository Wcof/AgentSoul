# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- 自动回滚机制已经添加到 `install.py`，现在安装失败会自动清理，不会留下部分文件。
- 当前所有功能都已闭环，注入失败处理从"用户手动清理"进化为"自动回滚"，用户体验提升。
- 当前最值得继续放大的最小闭环：Web UI 检查报告展示体验优化，为不同级别问题添加颜色标识提升可读性。
- 当前最值得继续精简的最小链路：检查是否有重复的 JSON schema 验证代码在不同 CLI 检查器中，可以统一收敛到 `common/health_gate.py`。

## 本轮新增功能的延续方向

- 自动回滚机制已经完整实现，用户现在可以安全重试安装，不用担心失败留下脏文件。
- 后续可以考虑：添加 `--rollback` 命令允许用户手动触发回滚到上一次安装状态，保留安装失败快照。
- 可以考虑为 `entry_detect.py --precheck` 添加更详细的冲突分析，告诉用户哪些文件会被覆盖。

## 本轮精简链路的继续优化方向

- 已经验证当前代码没有明显重复，统一 health_gate 已经提供了 `HealthSummary` 和 `UnifiedCheckResult` 共享类型，所有三个 CLI 检查器都已经在使用。
- 继续保持每次迭代一次小精简，保持代码干净。

## 本轮执行方式迭代的延续方向

- 本次迭代在熵注入模式下成功探索了"失败自动原子回滚"概念，提升了安装可靠性。
- 继续坚持"失败自动回滚"原则，任何增量写入操作都应该可追踪，失败时可以撤销。
- 下一次可以继续探索其他操作的可回滚性，比如配置升级。

## 必做项（本次遗留 + 卡死项追踪）

- 保持所有测试通过，健康度满分。
- 暂无遗留卡死项。

## 二选一战略方向（下次按健康度择一）

下次迭代健康度 100，熵注入不触发，正常选择偏稳定：

- **A 偏稳定**：选择已存在功能做小步深化，选择 Web UI 检查报告展示优化
- **B 偏破局**：若触发熵注入，可以探索"手动回滚到上一次安装"命令

## 功能候选 3 项

1. **（历史功能深化）** Web UI 陪伴检查报告展示优化：美化五项指标展示样式，添加状态颜色标识，提升可读性 → 延续统一健康闸门体系闭环，让可视化体验更好
2. **（历史功能深化）** Web UI 健康检查报告展示优化：为不同问题级别（error/warning/info）添加不同颜色背景标识，提升问题辨识度 → 延续统一健康闸门体系闭环，让可视化体验更好
3. **（自动升级探索）** 为 install.py 添加 `--rollback` 手动回滚命令，列出最近安装尝试并允许回滚 → 执行方式自我迭代，提升安装失败后的恢复能力

## 本次踩坑与陷阱警告

- ruff 会提示未使用导入对于仅用于副作用的导入（如 `readline`），需要手动添加 `# noqa: F401` 禁止警告。
- 当在全局作用域已经导入 `yaml`，内部函数中重复导入会导致 ruff 错误"referenced before assignment"，即使代码运行正确，最好还是移除重复导入保持 ruff 干净。
- 实验分支合并后记得删除分支，保持分支列表干净。

## 本次为熵注入 → 新方向初步结论

- 注入失败自动回滚机制探索成功：实现完整，测试全部通过，用户现在可以安全失败，自动恢复。
- 该功能对安装可靠性提升明显，可以保留，属于有用探索。
