# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- 统一健康闸门体系已经完整闭环：schema 定义 → 三个检查器实现 → JSON Schema → GitHub Action CI 示例 → Web UI 可视化导入 → README 文档完整说明，整个链路现在文档与代码完全同步。
- 两个核心 CI 工作流都已添加 `mypy --strict` 和 `ruff check` 静态检查，代码质量自动化验证完整。
- `.soul_health.md` 所有记录都已格式标准化，括号全部匹配，满足自动化读取要求。
- `.phase_summary.md` 所有阶段记录也已格式标准化，阶段性自审可以直接读取数据做趋势分析。
- 当前最值得继续放大的最小闭环：验证陪伴连续性检查报告在 Web UI 中也能正确适配标准 JSON Schema，确保两种检查报告都能正常可视化。
- 当前最值得继续精简的最小链路：检查其他文档是否还有描述不一致，保持所有文档与实际能力同步。

## 本轮新增功能的延续方向

- 延续"统一健康闸门体系"这一成果，现在完整链路 CLI → JSON → Web UI → 文档说明已经全部闭环，任何新检查只要遵循标准 Schema 就能直接在 Web UI 中可视化展示。
- 延续"纯静态 Web UI"架构，继续保持不需要后端服务，用户直接浏览器打开就能使用，架构简洁易部署。

## 本轮精简链路的继续优化方向

- 已经完成 README 文档补充，现在文档准确反映了 Web UI 的全部功能。
- 如果发现其他文档（如 CLAUDE.md 或 .capability_map.md）对新增功能描述不完整，继续更新同步，保持所有文档一致性。

## 本轮执行方式迭代的延续方向

- 继续强化"功能实现后文档必须同步"，代码实现 + 文档更新才算真正完成功能闭环。
- 继续坚持"小步增量"，每次只补充一个缺失的文档说明，保持改动可控，测试通过率持续 100%。
- 继续坚持"所有核心流程都要有 CI 验证"，减少人工失误，保证质量一致。

## 必做项（本次遗留 + 卡死项追踪）

- 保持统一健康体系闭环可用，不要让后续配套文档与实际能力脱节。
- 继续遵守"新增功能 + 上轮精简 + 执行方式迭代"三落点结构，不允许退化成仅验证、仅记录或只做表面措辞调整。

## 二选一战略方向（下次按健康度择一）

### A 偏稳定

- 检查 `.capability_map.md` 文档，确认已实现功能描述与当前实际一致，更新任何过时描述。
- 验证陪伴连续性检查报告 JSON 导入在 Web UI 中能正确解析显示，确认两种检查报告都工作正常。

### B 偏破局

- 如果发现其他文档格式不一致，可以继续检查并统一标准化，保证所有历史记录文档格式一致方便自动化读取。
- 或探索其他 Web UI 小功能增强，比如为报告添加导出功能。

## 功能候选 3 项

1. 历史功能深化：验证陪伴连续性检查报告在 Web UI 中也能正确适配标准统一 JSON Schema，确保两种检查报告都能正常导入和可视化展示。
2. 历史功能深化：检查 `.capability_map.md` 更新，确保已实现功能描述与当前实际能力一致。
3. 自动升级探索候选：检查是否有其他文档存在格式不一致，如有则继续统一标准化，保证所有历史记录干净可读。

## 本次踩坑与陷阱警告

- JSON Schema 定义标准格式后，不仅需要更新消费者代码，还需要更新文档说明，否则用户不知道新增功能可以使用。
- 在错误目录执行 mypy 会导致"找不到 Python 文件"错误，执行命令前需要确认当前工作目录正确。

## 本次是否熵注入

- 本次迭代 116 不是 3 的倍数，未触发熵注入。
