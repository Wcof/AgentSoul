# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- 安装回滚机制完整闭环已经完成：自动回滚处理安装失败/中断，手动回滚允许用户主动撤销安装。现在安装过程已经完全原子化：要么成功完成，要么完全回滚到安装前。
- 当前所有功能都已闭环，回滚机制完整。
- 下一个最值得继续放大的最小闭环：Web UI 记忆列表性能优化，当加载大量记忆文件时实现虚拟化只渲染可见项。

## 本轮新增功能的延续方向

- `install.py --rollback` 手动回滚已完整实现：
  - 每次成功安装自动保存变更记录到 `.install-tracker.json`
  - 最多保留最近 10 条记录，避免无限增长
  - 用户运行 `--rollback` 可以查看历史并选择回滚
  - 复用现有的 `InstallationRollback` 回滚逻辑，删除新建文件、恢复修改文件
  - 回滚成功后自动从历史中移除该记录
- 用户现在可以：安装失败自动回滚 → 改变主意手动回滚 → 重新安装，整个过程干净可靠，不会残留文件。

## 本轮精简链路的继续优化方向

- 已经验证当前代码没有明显重复，新增功能完全复用了已有的 `InstallationRollback` 类，没有重复代码。
- 继续保持每次迭代一次小精简，保持代码干净。

## 本轮执行方式迭代的延续方向

- 本次迭代完成了"自动回滚 → 手动回滚"完整闭环，安装流程现在完全满足原子性要求：要么完全成功，要么完全回滚。
- 继续坚持"失败自动恢复"原则，任何增量写入操作都可追踪，失败或用户主动撤销都能恢复。

## 必做项（本次遗留 + 卡死项追踪）

- 保持所有测试通过，健康度满分。
- 暂无遗留卡死项。

## 二选一战略方向（下次按健康度择一）

下次迭代健康度 100，135 不整除 3 不触发熵注入，正常选择偏稳定：

- **A 偏稳定**：选择已存在功能做小步深化，Web UI 记忆列表虚拟化性能优化
- **B 偏破局**：若触发熵注入，可以探索记忆文件批量去重清理功能

## 功能候选 3 项

1. **（历史功能深化）** Web UI 记忆列表性能优化：当加载大量记忆文件时，实现虚拟化只渲染可见项 → 改善加载数十个记忆文件后的滚动性能，提升大型记忆库使用体验
2. **（历史功能深化）** 检查并清理重复的 JSON schema 验证代码 → 继续精简统一健康闸门体系，确保所有检查器使用一致的验证逻辑
3. **（自动升级探索）** 为 `entry_detect.py` 添加 URL 分享检测，识别当前环境是否在分享链接会话中，自动加载对应搜索条件 → 提升 URL 分享功能用户体验

## 本次踩坑与陷阱警告

- ruff 会提示未使用导入对于仅用于副作用的导入（如 `readline`），需要手动添加 `# noqa: F401` 禁止警告。
- 当在全局作用域已经导入 `yaml`，内部函数中重复导入会导致 ruff 错误"referenced before assignment"，即使代码运行正确，最好还是移除重复导入保持 ruff 干净。
- 实验分支合并后记得删除分支，保持分支列表干净。
- 添加新功能/新命令到 argparse 时，注意不要重复定义 main() 函数，会导致语法错误。编辑前确认文件结构避免重复定义。

## 本次为正常 → 结论

- `install.py --rollback` 手动回滚实现完成，和已有的自动回滚配合，完整闭环安装原子性。该功能对安装可靠性提升明显，用户可以放心尝试安装，随时可以回滚，属于有用改进，可以保留。
