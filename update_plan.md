# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- entry_detect --precheck 增强已经完成：详细冲突分析，区分用户文件和 AgentSoul 文件，自动评分提醒，提示备份。现在预演功能完整闭环。
- 当前所有功能都已闭环，预演→确认→安装链路更透明。
- 下一个最值得继续放大的最小闭环：可以考虑为 install.py 添加 `--rollback` 手动回滚命令，列出最近安装尝试并允许选择回滚。

## 本轮新增功能的延续方向

- entry_detect --precheck 增强已完整实现：
  - 扩展了候选文件列表包含所有安装会触及的文件
  - 自动检测区分：用户原有文件 vs AgentSoul 自有文件
  - 用户原有文件会被扣分并明确提醒备份
  - AgentSoul 自有文件被视为正常更新不扣分
  - 详细描述每个文件的用途，让用户清楚知道是什么文件
  - 遵循统一 HealthSummary JSON schema，可被 Web UI 解析展示

- 用户现在运行 `entry_detect.py --precheck` 可以清楚看到：哪些自己的文件会被覆盖，需要备份；哪些是 AgentSoul 自己的文件，可以正常更新。这提升了安装透明度和安全性。

## 本轮精简链路的继续优化方向

- 已经验证当前代码没有明显重复，统一 health_gate 已经提供了 `HealthSummary` 和 `UnifiedCheckResult` 共享类型，本次增强也复用了这些类型，没有新增重复代码。
- 继续保持每次迭代一次小精简，保持代码干净。

## 本轮执行方式迭代的延续方向

- 本次迭代在正常模式下完成了既定的历史功能深化，验证了"逐步增强用户可见的安全提醒"策略有效。
- 继续坚持"安装前预演→冲突分析→提醒备份→用户确认"这一安全链路，减少意外覆盖。

## 必做项（本次遗留 + 卡死项追踪）

- 保持所有测试通过，健康度满分。
- 暂无遗留卡死项。

## 二选一战略方向（下次按健康度择一）

下次迭代健康度 100，如果 135 不是 3 的倍数，不触发熵注入，正常选择偏稳定：

- **A 偏稳定**：选择已存在功能做小步深化，为 install.py 添加 `--rollback` 手动回滚命令
- **B 偏破局**：若触发熵注入，可以探索记忆文件批量去重清理功能

## 功能候选 3 项

1. **（自动升级探索）** 为 install.py 添加 `--rollback` 手动回滚命令，列出最近安装尝试并允许选择回滚 → 执行方式自我迭代，提升安装失败后的恢复能力，延续自动回滚机制的完整闭环
2. **（历史功能深化）** Web UI 记忆列表性能优化：当加载大量记忆文件时，实现虚拟化只渲染可见项 → 针对高级用户加载数月记忆时的性能提升，改善大型记忆库使用体验
3. **（历史功能深化）** 检查并统一所有 CLI 检查器的帮助信息措辞，确保 `--summary-json` 描述一致 → 继续精简统一健康闸门体系，减少文档不一致

## 本次踩坑与陷阱警告

- ruff 会提示未使用导入对于仅用于副作用的导入（如 `readline`），需要手动添加 `# noqa: F401` 禁止警告。
- 当在全局作用域已经导入 `yaml`，内部函数中重复导入会导致 ruff 错误"referenced before assignment"，即使代码运行正确，最好还是移除重复导入保持 ruff 干净。
- 实验分支合并后记得删除分支，保持分支列表干净。
- 在项目根目录运行 Python 脚本，需要确保 `cd` 正确，否则会找错文件路径。

## 本次为正常 → 结论

- entry_detect --precheck 冲突分析增强完成，区分用户文件和 AgentSoul 文件，提示备份，提升安装透明度。该优化对安装安全性提升明显，属于有用改进，可以保留。
