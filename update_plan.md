# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- 统一健康闸门体系已经 **完整闭环**：JSON Schema 定义 → `health_check.py` / `entry_detect.py` / `companionship_checker.py` 三个 CLI 检查器全部输出标准 HealthSummary JSON 格式 → GitHub Actions CI 闸门示例 → Web UI 三种检查报告统一导入可视化，整个链路完全通畅。
- Web UI 灵魂成长曲线功能完整闭环：导入 `.soul_health.md` → 绘制曲线图 → 五项统计 → 自动趋势评估 → 导出文字/图片分享，整个用户流程通畅。
- entry_detect.py 入口探测完整闭环：环境检测 → 输出可用注入方法 → `--generate-template` 生成模板 → `--precheck` 预演检查冲突，完整用户注入路径支持。
- MCP 订阅推送机制完整闭环：创建订阅 → 推送事件 → 指数退避重试 → 失败计数自动退订 → 列出完整订阅状态，整个生命周期管理完成。
- 两个核心 CI 工作流都已添加 `mypy --strict` 和 `ruff check` 静态检查，代码质量自动化验证完整。
- `.soul_health.md` 所有记录都已格式标准化，括号全部匹配，满足自动化读取要求。
- `.phase_summary.md` 所有阶段记录格式也已标准化，阶段性自审可以直接读取数据做趋势分析。
- `.capability_map.md` 已更新，技术债描述准确反映当前实际情况。
- `CLAUDE.md` 已经完整反映最新项目架构和功能，不需要更新。
- 检查确认代码干净，没有明显的重复调用、重复分支或重复输出路径需要精简。
- 当前所有功能都已闭环，没有明显薄弱环节，最值得继续放大的最小闭环：完善 entry_detect，检测后自动生成 `starter-prompt.md` 完整启动提示文件；最值得继续精简的最小链路：确认 TypeScript 构建输出没有冗余，保持 MCP 服务器构建简洁。

## 本轮新增功能的延续方向

- 延续"统一健康闸门体系"成果，现在三个核心 CLI 检查器全部支持标准 JSON 输出，Web UI 可以统一处理所有检查报告可视化，架构一致性得到提升。
- 延续"标准化"策略，统一格式让消费者（Web UI）不需要为每种检查写特殊适配代码，降低了维护成本。

## 本轮精简链路的继续优化方向

- 已经检查确认上轮 entry_detect.py 新增 `--precheck` 代码干净，没有明显重复代码需要精简。
- 如果未来发现重复代码，可以继续收敛合并减少分叉。

## 本轮执行方式迭代的延续方向

- 继续强化"统一标准格式"策略，所有机器可读输出都使用统一 HealthSummary JSON Schema，减少格式分叉，让自动化和可视化更容易。
- 继续坚持"小步增量"，每次只处理一个小功能点，保持改动可控，测试通过率持续 100%。
- 继续坚持"所有核心流程都要有 CI 验证"，减少人工失误，保证质量一致。
- 继续坚持"阶段性自审每 5 轮一次"，用数据驱动战略调整，保持项目方向正确。

## 必做项（本次遗留 + 卡死项追踪）

- 保持统一健康体系闭环可用，不要让后续配套文档与实际能力脱节。
- 暂无遗留卡死项。

## 二选一战略方向（下次按健康度择一）

下次迭代是 129，是 3 的倍数，**触发熵注入模式**，自动选择 B 偏破局，选择一个适中规模的新探索方向。

## 功能候选 3 项

1. **（历史功能深化）** 完善 entry_detect 增强：检测后根据预演结果自动生成 `starter-prompt.md` 完整启动提示文件 → 进一步降低新用户使用门槛
2. **（历史功能深化）** Web UI 陪伴检查报告展示优化：美化五项指标展示样式，添加状态颜色标识，提升可读性 → 延续统一健康闸门体系闭环，让可视化体验更好
3. **（自动升级探索）** 探索失败注入自动回滚机制：注入失败后自动清理脏配置，恢复到注入前状态 → 执行方式自我迭代，提升注入失败后的恢复能力

## 本次踩坑与陷阱警告

- 统一帮助信息措辞时，需要检查三个检查器的描述一致，让用户获得一致的使用体验。
- 添加新 CLI 选项后，帮助信息必须同步更新，否则用户找不到新增选项。

## 本次是否熵注入

- 本次迭代 128 不触发熵注入。
