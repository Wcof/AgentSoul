# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- Web UI 灵魂成长曲线功能已经完整闭环：导入 `.soul_health.md` → 绘制健康度曲线图 → 显示五项核心统计 → 自动生成趋势评估 → 导出文字/图片分享，整个用户流程通畅，用户可以完成从导入到分享的全流程操作。
- MCP 订阅推送机制完整闭环：创建订阅 → 推送事件 → 指数退避重试 → 失败计数 → 超过次数自动退订 → 列出完整订阅状态，整个生命周期管理完成。
- entry_detect.py 完整闭环：环境检测 → 报告可用注入方法 → 直接生成注入模板输出，用户检测环境后可以直接复制使用。
- 两个核心 CI 工作流都已添加 `mypy --strict` 和 `ruff check` 静态检查，代码质量自动化验证完整。
- `.soul_health.md` 所有记录都已格式标准化，括号全部匹配，满足自动化读取要求。
- `.phase_summary.md` 所有阶段记录格式也已标准化，阶段性自审可以直接读取数据做趋势分析。
- `.capability_map.md` 已更新，技术债描述准确反映当前实际情况。
- `CLAUDE.md` 已经完整反映最新项目架构和功能，不需要更新。
- 检查确认代码干净，没有明显的重复调用、重复分支或重复输出路径需要精简。
- 当前所有功能都已闭环，没有明显薄弱环节，最值得继续放大的最小闭环：探索注入预演模式，在实际注入前生成预演报告检查冲突；最值得继续精简的最小链路：确认 TypeScript 构建输出没有冗余，保持 MCP 服务器构建简洁。

## 本轮新增功能的延续方向

- 延续"Web UI 灵魂成长曲线逐步增量深化"成果，从导入展示到自动评估再到导出分享，现在完整用户流程闭环，用户体验流畅。
- 延续"纯静态 Web UI"架构，所有导出功能都使用浏览器原生 API 实现（Blob URL、canvas），不需要引入任何框架或后端服务，保持架构简洁易维护。

## 本轮精简链路的继续优化方向

- 已经检查确认上轮 MCP 订阅推送新增代码干净，没有明显重复代码需要精简。
- 如果未来发现重复代码，可以继续收敛合并减少分叉。

## 本轮执行方式迭代的延续方向

- 继续强化"用户少一步手动操作"改进方向，能自动生成的就自动生成，能导出保存的就允许导出，减少用户手动复制或截图。
- 继续坚持"小步增量"，每次只处理一个小功能点，保持改动可控，测试通过率持续 100%。
- 继续坚持"所有核心流程都要有 CI 验证"，减少人工失误，保证质量一致。
- 继续坚持"阶段性自审每 5 轮一次"，用数据驱动战略调整，保持项目方向正确。

## 必做项（本次遗留 + 卡死项追踪）

- 保持统一健康体系闭环可用，不要让后续配套文档与实际能力脱节。
- 暂无遗留卡死项。

## 二选一战略方向（下次按健康度择一）

下次迭代是 127，不是 3 的倍数也不是 5 的倍数，按实际健康度和熵检测结果选择：
- A 偏稳定：选择一个已存在功能做小步深化，保持健康度满分
- B 偏破局：若触发熵注入，选择一个适中规模的新探索方向

## 功能候选 3 项

1. **（自动升级探索）** 探索注入预演模式：在不写入真实配置前预览 Master Agent 的 Persona/Memory/Skill 将如何进入目标工具入口，检查冲突 → 执行方式自我迭代，减少注入失败风险
2. **（历史功能深化）** 陪伴连续性检查器补齐：将结果也输出统一 HealthSummary JSON 格式，支持 Web UI 可视化 → 延续统一健康闸门体系，让陪伴检查也能在 Web UI 展示
3. **（历史功能深化）** entry_detect 增强：检测后自动检查 AgentSoul 安装状态，生成 `starter-prompt.md` 完整启动提示文件 → 进一步降低新用户使用门槛

## 本次踩坑与陷阱警告

- 导出 SVG 到 PNG 需要注意缩放质量，使用 2x 缩放保证导出图片清晰，直接使用 SVG 原始尺寸会显得模糊。
- 新增按钮在 HTML 中添加后，必须在 JavaScript 更新统计时启用按钮，初始状态应该禁用，没有数据时不可点击。

## 本次是否熵注入

- 本次迭代 126 不触发熵注入。
