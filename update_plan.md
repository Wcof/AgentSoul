# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- 统一健康闸门体系已经完整闭环：schema 定义 → 三个检查器实现 → JSON Schema → GitHub Action CI 示例 → **Web UI 可视化导入**，整个链路从 CLI 检查到浏览器展示完整可用。
- 两个核心 CI 工作流都已添加 `mypy --strict` 和 `ruff check` 静态检查，代码质量自动化验证完整。
- `.soul_health.md` 所有记录都已格式标准化，括号全部匹配，满足自动化读取要求。
- `.phase_summary.md` 所有阶段记录格式也已标准化，阶段性自审可以直接读取数据做趋势分析。
- 当前最值得继续放大的最小闭环：检查 README 文档，确保反映最新的 CI 和静态检查能力以及新增的 Web UI 健康报告可视化功能。
- 当前最值得继续精简的最小链路：统一健康体系已经完整，可检查其他文档是否存在格式不一致，继续保持所有历史记录格式统一。

## 本轮新增功能的延续方向

- 延续"统一健康闸门体系"这一成果，现在 CLI → JSON Schema → Web UI 可视化完整闭环，任何新添加的检查器只要遵循标准 JSON Schema 输出，Web UI 就能直接导入展示，不需要修改解析代码。
- 延续"Web UI 纯静态实现"模式，继续保持不需要后端服务，用户直接浏览器打开就能使用，架构简洁易部署。

## 本轮精简链路的继续优化方向

- 已经完成健康检查 JSON 输出格式与解析代码对齐，消除了格式分叉。
- 如果发现其他文档（如 .capability_map.md、README.md）对新增功能描述不完整或格式不一致，继续统一修正，保证所有文档与实际能力同步。

## 本轮执行方式迭代的延续方向

- 继续强化"数据格式标准化先于可视化消费"，先定义标准 Schema，再让生产者和消费者都对齐，这样新增消费者时改动最小。
- 继续坚持"小步增量"，即使已有功能实现，发现不兼容标准格式也只修改解析函数，不重构整个模块，保持改动可控，测试通过率持续 100%。
- 继续坚持"所有核心流程都要有 CI 验证"，减少人工失误，保证质量一致。

## 必做项（本次遗留 + 卡死项追踪）

- 保持统一健康体系闭环可用，不要让后续配套文档与实际能力脱节。
- 继续遵守"新增功能 + 上轮精简 + 执行方式迭代"三落点结构，不允许退化成仅验证、仅记录或只做表面措辞调整。

## 二选一战略方向（下次按健康度择一）

### A 偏稳定

- 检查 README 文档，更新内容反映最新 CI 静态检查能力和新增的 Web UI 健康检查报告可视化功能，确保文档与实际功能一致。
- 验证现有所有功能正常，确认没有隐性问题影响用户使用。

### B 偏破局

- （如果健康度仍保持 100 可选择）探索为 Web UI 添加陪伴连续性检查报告也适配标准统一 JSON Schema 格式（当前陪伴检查其实已经适配，可验证是否需要进一步调整）。
- 或探索其他新的 Web UI 增强功能。

## 功能候选 3 项

1. 历史功能深化：检查 README 文档更新，反映最新 CI 静态检查能力和 Web UI 新增的健康检查报告可视化功能，确保文档与代码同步。
2. 历史功能深化：检查其他文档（.capability_map.md 之外）是否存在格式不一致，如有则统一标准化。
3. 新功能探索（自动升级探索候选）：验证陪伴连续性检查报告在 Web UI 中也能正确导入标准 JSON Schema 格式，确保两种检查报告都能正常可视化。

## 本次踩坑与陷阱警告

- JSON Schema 定义标准格式后，需要确保消费者代码也更新适配，不能只更新生产者而消费者还是旧格式解析，会导致用户无法使用。
- 在错误目录执行查找会找不到测试文件，导致误认为测试文件不存在，执行命令前需要确认当前工作目录正确。

## 本次是否熵注入

- 本次迭代 115 不是 3 的倍数，未触发熵注入。
