# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- 统一健康闸门体系已经完整闭环：schema 定义 → 三个检查器实现 → JSON Schema → GitHub Action CI 示例 → Web UI 两种检查报告（健康检查/陪伴连续性检查）都适配标准格式可视化 → README 文档完整说明，整个链路现在完全闭环可用。
- 两个核心 CI 工作流都已添加 `mypy --strict` 和 `ruff check` 静态检查，代码质量自动化验证完整。
- `.soul_health.md` 所有记录都已格式标准化，括号全部匹配，满足自动化读取要求。
- `.phase_summary.md` 所有阶段记录格式也已标准化，阶段性自审可以直接读取数据做趋势分析。
- 当前最值得继续放大的最小闭环：检查 `.capability_map.md` 确认已实现功能描述与当前实际一致，更新任何过时描述。
- 当前最值得继续精简的最小链路：已经完成两种检查报告解析对齐标准 JSON Schema，没有剩余格式不一致，可检查其他文档是否需要格式统一。

## 本轮新增功能的延续方向

- 延续"统一健康闸门体系"这一成果，现在健康检查和陪伴连续性检查两种报告都已经适配标准 JSON Schema，Web UI 可以正确导入并可视化展示任意一种检查报告，后续新增检查器只要遵循标准 Schema 就能直接使用。
- 延续"纯静态 Web UI"架构，继续保持不需要后端服务，用户直接浏览器打开就能使用，架构简洁易部署。

## 本轮精简链路的继续优化方向

- 已经完成两种检查报告解析代码都对齐标准统一 JSON Schema，消除了格式分叉，现在用户使用体验一致，代码维护也更简单。
- 如果 `.capability_map.md` 中有描述过时，更新同步到当前实际能力。

## 本轮执行方式迭代的延续方向

- 继续强化"标准统一 Schema 对齐"，所有生产者和消费者都遵循同一标准，减少格式分叉和兼容性问题。
- 继续坚持"保持向后兼容"，格式升级时保留对旧格式的兼容处理，不破坏已有输出的使用流程。
- 继续坚持"所有核心流程都要有 CI 验证"，减少人工失误，保证质量一致。

## 必做项（本次遗留 + 卡死项追踪）

- 保持统一健康体系闭环可用，不要让后续配套文档与实际能力脱节。
- 继续遵守"新增功能 + 上轮精简 + 执行方式迭代"三落点结构，不允许退化成仅验证、仅记录或只做表面措辞调整。

## 二选一战略方向（下次按健康度择一）

### A 偏稳定

- 检查 `.capability_map.md`，确认已实现功能描述与当前实际一致，更新任何过时描述。
- 验证所有功能正常，确认统一健康闸门体系完整可用。

### B 偏破局

- （如果健康度仍保持 100 可选择）探索 Web UI 新增功能，比如为检查报告添加导出功能（导出为 PNG 图片或 Markdown）。
- 或探索其他文档格式标准化，继续保持所有历史记录格式一致。

## 功能候选 3 项

1. 历史功能深化：检查并更新 `.capability_map.md`，确保已实现功能描述与当前实际能力一致，更新任何过时信息。
2. 历史功能深化：检查其他文档（如 CLAUDE.md）是否存在描述不一致，如有则更新同步，保持所有文档与代码一致。
3. 自动升级探索候选：检查是否还有其他文档存在格式不一致，如有则继续统一标准化，保证所有历史记录干净可读。

## 本次踩坑与陷阱警告

- 统一 JSON Schema 定义后，需要确保所有消费者都更新适配，不能只更新生产者而消费者还是旧格式解析，会导致用户无法正常使用。
- 需要保持向后兼容，旧格式输出仍然能正常解析，不破坏已有使用流程。

## 本次是否熵注入

- 本次迭代 117 不是 3 的倍数，未触发熵注入。
