# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- 统一健康闸门体系已经完整闭环：schema 定义 → 三个检查器实现 → JSON Schema → GitHub Action CI 示例 → Web UI 两种检查报告（健康检查/陪伴连续性检查）都适配标准格式可视化 → README 文档完整说明 → `.capability_map.md` 能力地图准确反映 → `CLAUDE.md` 确认完整反映最新架构，整个链路现在完全闭环可用。
- 两个核心 CI 工作流都已添加 `mypy --strict` 和 `ruff check` 静态检查，代码质量自动化验证完整。
- `.soul_health.md` 所有记录都已格式标准化，括号全部匹配，满足自动化读取要求。
- `.phase_summary.md` 所有阶段记录格式也已标准化，阶段性自审可以直接读取数据做趋势分析。
- `.capability_map.md` 已更新，技术债描述准确反映当前实际情况。
- `CLAUDE.md` 已经完整反映最新项目架构和功能，不需要更新。
- 检查确认代码干净，没有明显的重复调用、重复分支或重复输出路径需要精简。
- 当前所有功能都已闭环，没有明显薄弱环节，可以等待熵注入触发新探索或阶段性自审总结。

## 本轮新增功能的延续方向

- 延续"统一健康闸门体系"这一成果，现在完整链路 CLI → JSON → Web UI → 文档说明已经全部闭环可用，任何新检查只要遵循标准 Schema 就能直接在 Web UI 中可视化展示。
- 延续"纯静态 Web UI"架构，继续保持不需要后端服务，用户直接浏览器打开就能使用，架构简洁易部署。

## 本轮精简链路的继续优化方向

- 已经检查确认代码干净，没有明显重复代码需要精简。
- 如果未来发现重复代码，可以继续收敛合并减少分叉。

## 本轮执行方式迭代的延续方向

- 继续强化"文档必须及时更新反映代码变化"，每次功能完成后都要更新所有相关文档，让阅读者获得准确信息。
- 继续坚持"小步增量"，每次只处理一个小任务，保持改动可控，测试通过率持续 100%。
- 继续坚持"所有核心流程都要有 CI 验证"，减少人工失误，保证质量一致。

## 必做项（本次遗留 + 卡死项追踪）

- 保持统一健康体系闭环可用，不要让后续配套文档与实际能力脱节。
- 继续遵守"新增功能 + 上轮精简 + 执行方式迭代"三落点结构，不允许退化成仅验证、仅记录或只做表面措辞调整。

## 二选一战略方向（下次按健康度择一）

下次迭代是 120，120 是 5 的倍数（120/5=24），会自动触发阶段性自审，不需要提前选择，等待自审触发后再决策。

## 功能候选 3 项（下次自审后执行）

（保留空位，等待下次阶段性自审后更新）

## 本次踩坑与陷阱警告

- CLAUDE.md 需要定期检查确认反映最新架构，如果描述过时会误导后续执行。
- 在错误目录执行 mypy 会导致"找不到 Python 文件"错误，执行命令前需要确认当前工作目录正确。

## 本次是否熵注入

- 本次迭代 119 不是 3 的倍数，未触发熵注入。
