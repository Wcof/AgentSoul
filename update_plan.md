# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- 统一健康闸门体系已经完整闭环：schema 定义 → 三个检查器实现 → JSON Schema → GitHub Action CI 示例 → Web UI 两种检查报告（健康检查/陪伴连续性检查）都适配标准格式可视化 → README 文档完整说明 → `.capability_map.md` 能力地图准确反映 → `CLAUDE.md` 确认完整反映最新架构，整个链路现在完全闭环可用。
- 两个核心 CI 工作流都已添加 `mypy --strict` 和 `ruff check` 静态检查，代码质量自动化验证完整。
- `.soul_health.md` 所有记录都已格式标准化，括号全部匹配，满足自动化读取要求。
- `.phase_summary.md` 所有阶段记录格式也已标准化，阶段性自审可以直接读取数据做趋势分析。
- `.capability_map.md` 已更新，技术债描述准确反映当前实际情况。
- `CLAUDE.md` 已经完整反映最新项目架构和功能，不需要更新。
- 检查确认代码干净，没有明显的重复调用、重复分支或重复输出路径需要精简。
- entry_detect.py 现在完整闭环：环境检测 → 报告可用注入方法 → 自动生成可复制模板，整个用户路径通畅。
- 当前所有功能都已闭环，没有明显薄弱环节，最值得继续放大的最小闭环：MCP 订阅推送机制补齐断连重试和状态可观测，提升 Master Agent 事件通知连续性；最值得继续精简的最小链路：确认所有文档与代码同步，保持描述一致。

## 本轮新增功能的延续方向

- 延续"入口能力探测"这一成果，现在 entry_detect 从"识别环境"完整闭环到"输出可用模板"，任何新环境下用户都能一步获得正确注入指引。
- 延续"CLI 工具先补机器可读再补用户便利"的深化顺序，这个顺序让自动化和人工使用都得到满足，策略正确值得继续沿用。

## 本轮精简链路的继续优化方向

- 已经检查确认 Web UI 新增代码干净，没有明显重复代码需要精简。
- entry_detect.py 新增代码符合现有风格，没有重复需要合并。
- 如果未来发现重复代码，可以继续收敛合并减少分叉。

## 本轮执行方式迭代的延续方向

- 继续强化熵注入模式选择适中规模探索，不贪大求全，一个小功能完整闭环，保持测试全程全绿。
- 继续坚持"用户少一步操作"的改进方向，能自动化生成的就不要让用户手动找。
- 继续坚持"先机器可读，后用户便利"的 CLI 工具深化顺序，这个顺序保证自动化链路先稳定，再优化人机交互。
- 继续遵守"新增功能 + 上轮精简 + 执行方式迭代"三落点结构，不允许退化成仅验证、仅记录或只做表面措辞调整。

## 必做项（本次遗留 + 卡死项追踪）

- 保持统一健康体系闭环可用，不要让后续配套文档与实际能力脱节。
- MCP 订阅推送机制现在只有基本实现，还缺断连重试、推送状态可观测、订阅生命周期管理，需要补齐。

## 二选一战略方向（下次按健康度择一）

下次迭代是 124，不是 5 的倍数，按实际健康度和熵检测结果选择：
- A 偏稳定：选择一个已存在功能做小步深化，保持健康度满分
- B 偏破局：若触发熵注入，选择一个适中规模的新探索方向

## 功能候选 3 项（下次自审后执行）

1. **（历史功能深化）** MCP 订阅推送机制补齐：补充断连重试、推送状态可观测、订阅生命周期管理 → 提升 Master Agent 状态变化通知连续性
2. **（历史功能深化）** Web UI 成长曲线补充：添加导出趋势评估图片/文字功能，方便用户分享结果 → 承接本轮 Web UI 趋势评估功能，让用户可以保存分享
3. **（自动升级探索）** 探索失败注入自动回滚机制：注入失败后自动清理脏配置，恢复到注入前状态 → 执行方式自我迭代，提升注入失败后的恢复能力

## 本次踩坑与陷阱警告

- mypy 对动态字典访问需要显式类型断言，不能偷懒跳过，否则 strict 模式会报错。
- 新增 CLI 选项后，帮助信息必须同步更新，保持文档与代码一致。

## 本次是否熵注入

- 本次迭代 123 触发熵注入，探索自动生成注入模板成功。
