# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- 本轮按破局模式做结构性深化：`companionship_checker` 新增 `--summary-json` 机器可读摘要，门控字段可直接被 CI/脚本消费。
- 最薄弱环节：`git stash` 在沙箱内会因 `.git/index.lock` 权限被拒而失败，需继续把协议执行链路做成“自动识别并升级执行”的稳定机制。

## 圆内任务模板（本轮落地记录）

- Master Agent 能力：状态延续 / 用户可感知陪伴
- 工具入口：通用底座（CLI + 自动化脚本）
- 闭环结果：`companionship_checker --summary-json --min-score X` 可输出统一摘要并给出门控退出码
- 陪伴增益：把连续性检查从“人读报告”升级为“自动化可判定”，减少低连续性状态误发布
- 最小验证：新增 summary-json 门控通过/失败用例并全量回归

## 必做项（本次遗留 + 卡死项追踪）

- 继续收敛 `git stash` 在沙箱中的执行策略，避免再次成为预检阻塞点。
- 为 `companionship_checker` 增加 `--min-score` 非法值路径的端到端 CLI 验证。
- 评估 Python 3.9.6 与项目要求 `>=3.10` 的差异风险，给出迁移窗口建议。

## 二选一战略方向（下次按健康度择一）

### A 偏稳定

- 增加 CLI 参数异常输入的错误语义与退出码一致性检查。
- 在 README 增加 CI 场景示例（如何消费 `--summary-json`）。

### B 偏破局

- 将连续性门控摘要抽象为通用“健康门控接口”，复用到 `health_check`/`entry_detect` 等 CLI，形成统一自动化规范。

## 功能候选 3 项（2 深化 + 1 探索）

1. 历史功能深化：为 `companionship_checker` 增补非法阈值/冲突参数的 CLI 行为验证。
2. 历史功能深化：补齐文档中的自动化门控示例（本地脚本 + CI 入口）。
3. 新功能探索（自动升级探索候选）：定义跨检查器统一摘要 schema（score/status/reason/action）并做最小原型。

## 本次踩坑与陷阱警告

- 沙箱环境下，`git stash` 可能因 `.git/index.lock` 写权限受限失败；协议执行需要自动升级执行权限而非重复重试。
- 摘要 JSON 测试不能假设 stdout 只有 JSON，一些日志会先输出，解析应取最后一行摘要。

## 本次是否熵注入

- 本次迭代 `104` 非 3 的倍数，未触发熵注入。
