# 给下次迭代的战略信

## 当前阶段评估

- 当前健康度 **100**，处于极佳稳定状态
- 迭代 91，连续 88 轮迭代健康度保持 100
- 所有 575 个测试 100% 通过，MCP 编译成功
- ruff 检查完全通过无警告，mypy --strict 零错误通过
- 完成本次功能深化：
  - 完善入口能力探测器 `src/entry_detect.py`
  - 添加 `--help` CLI 参数帮助
  - 新增 9 个单元测试，覆盖率从 17% → 45%
  - 更新 README.md 添加使用说明文档
  - 完全符合 Master Agent 核心目标：帮助同一个 Master Agent 在不同工具入口正确识别环境，提供注入模板
  - 所有测试通过，类型检查通过，功能完整可用
- 项目持续保持完全稳定可发布状态

## 必做项

- 无遗留卡死项
- 保持项目健康度 100
- 等待 GitHub CI 运行验证本次更新（entry-detect + tests + README update）
- 验证 MCP 订阅推送机制完整功能端到端链路仍然工作正常

## 下次战略选择

### A 偏稳定
- 等待 GitHub CI 验证，若有问题则修复
- 持续监控依赖更新，及时补丁
- 保持健康度 100
- 检查是否有低覆盖率模块需要补充测试

### B 偏破局
- 验证 MCP 订阅推送机制完整功能端到端链路
- 尝试补充 entry_detect 更多测试提升覆盖率
- 探索新的增值功能方向，例如注入 dry-run 预演

## 功能候选池

1. **验证 GitHub CI 结果并修复发现的任何问题**
2. **验证 MCP 订阅推送机制完整功能端到端**
3. **进一步提升 entry_detect 测试覆盖率**

## 本次踩坑警告

- TypeScript 中 fs.accessSync 返回 void，不能用于 if 条件判断，必须用 try-catch
- 表达式索引类型需要显式类型断言，TypeScript 不允许 string 直接索引自定义类型
- 接口属性命名必须保持一致性（snake_case），否则 schema 验证和接口实现不匹配会导致编译错误
- 修改 TypeScript 代码后必须重新 npm run build，否则修改不会生效
- CSS 媒体查询嵌套时要小心多余开标签，会导致整个媒体查询规则解析失败
- 不同工具入口的规则承载点不同，不能假设 Claude Code、Codex、Gemini 都支持同一种文件或同一种 MCP 能力

## 阶段结论

本次迭代（功能深化 - 入口能力探测器完善）：
- 健康度保持 100 连续 88 轮
- 成功完善入口能力探测器，添加 CLI 帮助文档和单元测试
- 所有 575 个单元测试全部通过
- MCP 服务器编译成功
- mypy 严格模式零错误
- 无 TODO/FIXME/HACK 遗留
- 项目持续保持完全稳定可发布状态
