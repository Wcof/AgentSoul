# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- entry_detect.py 入口探测注入路径现在完整闭环：环境检测 → 输出可用注入方法 → `--generate-template` 生成可直接复制的注入模板 → `--precheck` 预演检查（检测现有配置文件冲突）→ `--write-starter` 自动生成完整 `starter-prompt.md` 启动文件，整个用户路径完整，用户只需要按步骤执行就能完成注入，使用门槛进一步降低。
- 统一健康闸门体系已经 **完整闭环**：JSON Schema 定义 → `health_check.py` / `entry_detect.py` / `companionship_checker.py` 三个 CLI 检查器全部输出标准 HealthSummary JSON 格式 → GitHub Actions CI 闸门示例 → Web UI 三种检查报告统一导入可视化，整个链路完全通畅。
- Web UI 灵魂成长曲线功能完整闭环：导入 `.soul_health.md` → 绘制曲线图 → 五项统计 → 自动趋势评估 → 导出文字/图片分享，整个用户流程通畅。
- MCP 订阅推送机制完整闭环：创建订阅 → 推送事件 → 指数退避重试 → 失败计数 → 超过次数自动退订 → 列出完整订阅状态，整个生命周期管理完成。
- 两个核心 CI 工作流都已添加 `mypy --strict` 和 `ruff check` 静态检查，代码质量自动化验证完整。
- `.soul_health.md` 所有记录都已格式标准化，括号全部匹配，满足自动化读取要求。
- `.phase_summary.md` 所有阶段记录格式也已标准化，阶段性自审可以直接读取数据做趋势分析。
- `.capability_map.md` 已更新，技术债描述准确反映当前实际情况。
- `CLAUDE.md` 已经完整反映最新项目架构和功能，不需要更新。
- 检查确认代码干净，没有明显的重复调用、重复分支或重复输出路径需要精简。
- 当前所有功能都已闭环，没有明显薄弱环节，整体项目进入非常健康稳定的状态。

## 本轮新增功能的延续方向

- 延续"entry_detect 入口探测自动升级"成果，从简单环境识别一步步完善到自动生成完整启动文件，用户入门体验越来越好，符合"降低使用门槛"的方向。
- 延续"熵注入选择适中规模"策略，每次探索一个适中规模新功能，保持架构简洁，测试持续全绿。

## 本轮精简链路的继续优化方向

- 已经检查确认上轮统一帮助信息措辞工作完成，三个 CLI 检查器帮助信息已经一致，没有重复不一致需要合并。
- 如果未来发现重复代码，可以继续收敛合并减少分叉。

## 本轮执行方式迭代的延续方向

- 继续强化熵注入模式每次探索一个适中规模新功能，不做大而全重构，保持改动可控测试全绿。
- 继续坚持"用户少一步手动操作"改进方向，能自动化生成的就自动生成，减少用户手动复制粘贴。
- 继续坚持"小步增量"，每次只处理一个小功能点，保持改动可控，测试通过率持续 100%。

## 必做项（本次遗留 + 卡死项追踪）

- 保持统一健康体系闭环可用，不要让后续配套文档与实际能力脱节。
- 暂无遗留卡死项。

## 二选一战略方向（下次按健康度择一）

下次迭代是 130，不是 3 的倍数也不是 5 的倍数，按实际健康度和熵检测结果选择：
- A 偏稳定：选择一个已存在功能做小步深化，保持健康度满分
- B 偏破局：若触发熵注入，选择一个适中规模的新探索方向

## 功能候选 3 项

1. **（历史功能深化）** Web UI 陪伴检查报告展示优化：美化五项指标展示样式，添加状态颜色标识，提升可读性 → 延续统一健康闸门体系闭环，让可视化体验更好
2. **（自动升级探索）** 探索失败注入自动回滚机制：注入失败后自动清理脏配置，恢复到注入前状态 → 执行方式自我迭代，提升注入失败后的恢复能力
3. **（历史功能深化）** 验证 `companionship_checker.py` 输出的 JSON 在 Web UI 正确显示 → 确认统一健康闸门体系工作正常

## 本次踩坑与陷阱警告

- 循环变量名和 with open 语句中的变量名重复会导致 mypy 类型检查错误，需要用不同变量名避免冲突。
- 添加新 CLI 选项后，帮助信息 `--help` 输出必须同步添加，否则用户找不到新增选项。

## 本次是否熵注入

- 本次迭代 129 触发熵注入，探索 entry_detect 自动生成启动模板成功。
