# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- 记忆文件批量去重清理功能已完成：为 `entry_detect.py` 添加 `--cleanup-memory` 命令，可以扫描记忆目录，检测重复/损坏/空的记忆文件，输出可读报告和统一 JSON 摘要。
- URL 分享检测功能完整闭环已经完成：Web UI 生成链接 → CLI 验证链接 → 用户分享 → 他人打开 Web UI 自动加载。
- 统一健康闸门体系精简已完成：三个 CLI 检查器（health_check、companionship_checker、entry_detect）四处 --summary-json 输出点都已统一复用公共函数 handle_summary_output，消除重复代码。
- Web UI 记忆列表性能优化已完成：默认只展开最新年月分组，收起分组采用懒渲染只在点击展开时才创建DOM节点。大幅减少初始DOM节点数，显著提升大型记忆库滚动性能。
- 安装回滚机制完整闭环已经完成：自动回滚处理安装失败/中断，手动回滚允许用户主动撤销安装。现在安装过程已经完全原子化：要么成功完成，要么完全回滚到安装前。
- 第 28 阶段阶段性自审已完成：验证近 5 轮均值健康度 99.2，趋势平稳高位。
- 自动升级探索编排器已完成（本次熵注入探索）：新增 `src/auto_explore.py`，扫描项目状态自动提出下一个探索候选，自动检查圆心约束确保不出圈。
- 当前所有计划候选功能都已完成，框架现在可以自动提出下一个探索候选。
- 下一个探索候选将由自动探索编排器提出。

## 本轮新增功能的延续方向

- 熵注入探索完成自动升级探索编排器：
  - 新增独立 CLI 工具 `src/auto_explore.py`
  - 自动扫描 `update_plan.md` 和 `.capability_map.md` 提取待探索候选
  - 自动检查四项圆心约束（Master Agent 不出圈），确认候选是否符合核心目标
  - 输出人类可读报告和统一 JSON 摘要，给出探索建议
  - 让框架具备自我观察和自动选题能力，进一步增强自主进化特性
- 这个能力符合 AgentSoul "持续自主进化"的目标，帮助框架自动发现下一个值得探索的方向。

## 本轮精简链路的继续优化方向

- 本次没有需要额外精简的重复代码，新增功能模块化良好，没有引入重复逻辑
- 复用了已有的 `handle_summary_output` 统一输出函数，保持健康闸门体系一致性
- 继续保持每次迭代一次小精简，保持代码干净。

## 本轮执行方式迭代的延续方向

- 本次迭代是熵注入探索，完成了对执行框架本身的增强：自动选题能力
- 继续坚持"小步增量"原则，每次补全一个小闭环，不搞大包大揽
- 自动选题能力让自迭代流程更自动化，减少手动选题工作，框架更自主。

## 必做项（本次遗留 + 卡死项追踪）

- 保持所有测试通过，健康度满分。
- 暂无遗留卡死项。

## 二选一战略方向（下次按健康度择一）

下次迭代健康度 100，142 不整除 3 不触发熵注入，由自动探索编排器给出建议：

- **A 偏稳定**：自动探索未发现待探索候选，等待新的自然需求
- **B 偏破局**：自动探索未发现待探索候选，等待新的探索方向

## 功能候选 3 项

1. **（历史功能深化）** ✓ Web UI 记忆列表性能优化：当加载大量记忆文件时，实现虚拟化只渲染可见项 → 改善加载数十个记忆文件后的滚动性能，提升大型记忆库使用体验 **已完成**
2. **（历史功能深化）** ✓ 检查并清理重复的 JSON schema 验证代码 → 继续精简统一健康闸门体系，确保所有检查器使用一致的验证逻辑 **已完成**
3. **（自动升级探索）** ✓ 为 `entry_detect.py` 添加 URL 分享检测，识别当前环境是否在分享链接会话中，自动加载对应搜索条件 → 提升 URL 分享功能用户体验 **已完成**
4. **（破局探索）** ✓ 记忆文件批量去重清理功能 → 扫描记忆目录，识别重复/损坏/空的记忆文件，提供清理选项 → 保持记忆库干净，减少无效文件 **已完成**
5. **（熵注入探索）** ✓ 自动升级探索编排器 → 从能力缺口、更新计划、能力地图中自动提出下一个探索候选，自动检查圆心约束 → 让框架自主发现下一个探索方向 **已完成**

## 下次功能候选 3 项（按格式要求 2 历史+1 自动升级）：

由 `src/auto_explore.py` 自动生成，当前无待探索候选，保留空位等待自动选题：

1. **（自动探索）** 等待下次迭代由 `auto_explore.py` 自动提出
2. **（历史功能深化）** （保留空位）
3. **（自动升级探索）** （保留空位）

## 本次踩坑与陷阱警告

- ruff 会提示未使用导入对于仅用于副作用的导入（如 `readline`），需要手动添加 `# noqa: F401` 禁止警告。
- 当在全局作用域已经导入 `yaml`，内部函数中重复导入会导致 ruff 错误"referenced before assignment"，即使代码运行正确，最好还是移除重复导入保持 ruff 干净。
- 实验分支合并后记得删除分支，保持分支列表干净。
- 添加新功能/新命令到 argparse 时，注意不要重复定义 main() 函数，会导致语法错误。编辑前确认文件结构避免重复定义。
- 增量扩展功能可能会产生函数副本（如 `renderMemoryList` 被原版本和标签筛选扩展版本各保存一份），修改功能时必须同时更新两个副本保持行为一致。
- 抽取公共函数后忘记添加导入会导致 NameError，修改后必须立即测试验证，不要等到提交前才发现导入缺失。
- Python 标准库 base64 解码不需要额外浏览器兼容函数，直接使用 `base64.b64decode` 就可以，不用找错实现。
- 读取能力地图时，"- 无"这类空条目需要过滤，否则会误判为有待探索候选，解析时需要跳过空条目。

## 本次为熵注入探索 → 结论

- 完成自动升级探索编排器的熵注入探索，AgentSoul 现在具备自动扫描项目状态并提出下一个探索候选的能力，且自动检查圆心约束确保始终服务于 Master Agent 灵魂延续核心目标。这是框架本身的一次自我增强，让自迭代流程更自动化。所有测试 638/638 通过，健康度保持满分，探索成功，可以保留。
