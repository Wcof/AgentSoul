# 给下次迭代的战略信

## 当前阶段评估

- 当前健康度 **100**，处于极佳稳定状态
- 迭代 60，连续 57 轮迭代健康度保持 100
- 完成第 **12** 阶段阶段性自审，近 5 次均值健康度 **100**，趋势**持续平稳满分保持**
- 所有 211 个测试 100% 通过，MCP 编译成功
- ruff 检查完全通过无警告
- mypy 严格类型检查持续全绿通过 → 代码质量持续保持高水准
- **所有规划的核心功能现已全部完成**！包括：
  - 基础人格/情绪工具 ✓
  - 分层记忆系统 ✓
  - 核心记忆工具 ✓
  - 实体记忆工具 ✓
  - KV-Cache 三层缓存 ✓
  - Soul Board 项目看板 ✓
  - Soul Board 自定义标签分类 ✓
  - 订阅推送机制 ✓
  - 版本管理与回滚 ✓
  - 写入人格配置 ✓
  - 健康检查 ✓
  - 分层归档一致性检查 ✓
  - 自适应学习完整暴露 ✓
  - 灵魂成长曲线可视化（MCP + Python CLI）✓
  - 健康度历史图表导出（SVG/JSON/PNG）✓
  - 健康度查询 MCP 工具 ✓
  - 纯静态 Web UI 可视化 ✓
  - Web UI 深色模式支持 ✓
  - Web UI 手动主题切换 ✓ 完成
  - Web UI 全文搜索功能 ✓
  - Web UI 批量导入导出功能 ✓
  - Web UI PNG 导出健康图表 ✓
  - Web UI 记忆在线编辑保存 ✓
  - Web UI 记忆分组折叠浏览 ✓ 完成
  - Web UI 键盘导航功能 ✓ 完成
  - Web UI 快速标签筛选 ✓ 完成
  - 补充单元测试覆盖边缘情况 ✓ 完成
  - 补充单元测试覆盖存储层 ✓ 完成
  - 补充单元测试覆盖增强记忆优先级模块 ✓ 本次完成
  - 修复 ruff 配置废弃警告 ✓ 完成
  - README 健康度徽章 ✓
  - 完整文档更新 ✓
  - README 添加 GitHub 徽章 ✓
  - README 完善新增功能说明 ✓
  - mypy 严格类型检查全绿通过 ✓ 完成
  - GitHub Actions CI 配置 ✓
  - 添加 ruff lint 检查 ✓ → 持续全绿通过

## 必做项

- 无遗留卡死项
- 可进一步为低覆盖率模块补充单元测试继续提升覆盖率：`src/health_check.py` (48%), `src/storage/mcp_client.py` (35%)
- 保持项目健康度 100

## 下次战略选择

### A 偏稳定
- 等待 GitHub CI 验证配置正确
- 如果 CI 报告任何错误，修复相应问题
- 持续监控依赖更新，及时补丁
- 保持健康度 100
- 可进一步为低覆盖率模块补充单元测试继续提升覆盖率

### B 偏破局
- 探索增值功能方向，例如：
  - 继续补充单元测试覆盖 `src/health_check.py`、`src/storage/mcp_client.py` 等低覆盖率模块进一步提升覆盖率
  - 探索新的可能的扩展功能
  - 检查并清理任何新发现的代码质量问题

## 功能候选池

1. **继续补充单元测试提升覆盖率**（针对低覆盖率模块如 `health_check`、`storage/mcp_client` 等）
2. **检查并清理任何新发现的代码质量问题**
3. **探索新的可能的扩展功能**

## 本次踩坑警告

- 2026 不是闰年 → 2026-02-29 不存在，闰年判断规则能被 4 整除但不能被 100 整除，或能被 400 整除
- 检测不到冲突时 `detect_conflict` 返回 `None`，测试代码不能假设它总是返回一个对象
- TypeScript 中 `fs.accessSync` 返回 void，不能用于 if 条件判断，必须用 try-catch
- 表达式索引类型需要显式类型断言，TypeScript 不允许 string 直接索引自定义类型
- 接口属性命名必须保持一致性（snake_case），否则 schema 验证和接口实现不匹配会导致编译错误
- 修改 TypeScript 代码后必须重新 `npm run build`，否则修改不会生效
- 正则表达式字面量必须正确加括号，`.match/` 会语法错误，必须是 `.match(/.../)`
- macOS date 命令参数格式与 GNU date 不同，脚本需要适配
- 对 Optional 类型要显式断言或者用保护语句，不能直接访问属性
- 泛型类型必须提供类型参数，`dict` 需要 `dict[K,V]`，`list` 需要 `list[T]`，否则 mypy --strict 会报错
- 使用 `subprocess.Popen` 时，类型参数要匹配 `text=True/False`，`text=True` → `Popen[str]`，`text=False` → `Popen[bytes]`
- Python 3.9+ 支持内置泛型语法 `list[T]`/`dict[K,V]`，不再需要从 `typing` 导入 `List`/`Dict`
- 创建测试文件时注意目录位置，不要误创建到 `mcp_server/tests` 子目录
- ruff 新版本已废弃顶层 `select`/`ignore` 配置，必须移动到 `[tool.ruff.lint]` 区段
- 编写单元测试时，访问计数计算需要仔细核对初始值，简单加法错误会导致测试失败
- invalidate_cache 后立即检查 `_priorities` 长度，它已经被清空，重载只在下次访问时发生

## 阶段结论

本次迭代（第十二阶段）：
- 健康度保持 100 连续 57 轮
- 所有 211 个单元测试全部通过
- MCP 服务器编译成功
- 完成第十二阶段阶段性自审，均值健康度 100，趋势持续平稳满分保持
- 熵注入模式为低覆盖率模块 `src/memory_enhanced/priority.py` 新增 29 个单元测试
- 覆盖率从 **26% → 95%**，提升了 **69 个百分点**
- 总体覆盖率从 **65% → 68%**，提升了 3 个百分点
- 总测试数从 182 → 211，新增 29 个测试
- 增量式改进完全向后兼容，所有测试 100% 通过
- 无 TODO/FIXME/HACK 遗留
- 项目持续保持完全稳定可发布状态
