# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- 统一健康闸门体系已经完整闭环：schema 定义、三个检查器实现、JSON Schema 和 GitHub Action CI 示例都已到位。
- 静态检查 `mypy --strict src/` 零错误，`ruff check src/` 全绿，并且已经添加到 CI 自动验证。
- 当前最值得继续放大的最小闭环：探索 Web UI 导入并展示健康检查 JSON 报告，让用户更方便查看健康检查结果可视化。
- 当前最值得继续精简的最小链路：检查是否还有其他历史文档格式不统一，继续标准化保证自动化分析输入干净。

## 本轮新增功能的延续方向

- 延续"CI 添加静态检查门"这一成果，可以考虑在 companionship-check.yml 也添加静态检查门，保证所有 CI 工作流都验证静态质量。
- 延续"格式标准化"这一成果，继续检查其他历史文档是否存在格式不一致，统一所有记录格式方便自动化读取。

## 本轮精简链路的继续优化方向

- 继续验证 `.phase_summary.md` 格式一致性，保持现状已经很好，若发现局部不一致再修正。
- 若发现其他历史记录文档存在格式问题，继续统一标准化，减少协议传感器读取错误。

## 本轮执行方式迭代的延续方向

- 继续强化"自动化质量门"CI 理念，让静态质量验证在所有核心 CI 工作流都启用。
- 继续坚持"文档格式标准化先于自动化分析"的原则，保证传感器输入干净才能输出准确。
- 继续坚持"小步增量"，每次一个小新增 + 一个小精简，保持迭代稳定。

## 必做项（本次遗留 + 卡死项追踪）

- 保持统一健康体系闭环可用，不要让后续配套文档与实际能力脱节。
- 继续遵守"新增功能 + 上轮精简 + 执行方式迭代"三落点结构，不允许退化成仅验证、仅记录或只做表面措辞调整。

## 二选一战略方向（下次按健康度择一）

### A 偏稳定

- 在 companionship-check.yml CI 工作流中也添加 mypy --strict 和 ruff 静态检查，保证所有 CI 工作流都验证代码质量。
- 检查并清理任何剩余的格式不统一历史记录，保证所有文档格式一致。

### B 偏破局

- 探索实现 Web UI 直接导入并展示健康检查 JSON 报告，让用户可以在浏览器中可视化查看健康检查结果。
- 研究是否能让 Web UI 直接从本地文件读取 JSON 报告展示，不需要后端服务。

## 功能候选 3 项

1. 历史功能深化：在 companionship-check.yml CI 中添加 mypy --strict 和 ruff 静态检查门，统一所有 CI 工作流的质量验证。
2. 历史功能深化：检查所有历史记录文档（.soul_health.md, .phase_summary.md）确认格式统一，修正任何剩余不一致。
3. 新功能探索（自动升级探索候选）：探索 Web UI 导入并可视化健康检查 JSON 报告，用户选择 JSON 文件后在页面上展示结构化结果和图表。

## 本次踩坑与陷阱警告

- 在 cd 到子目录后运行命令，容易忘记返回项目根目录导致找不到文件，执行命令前需要确认当前工作目录正确。
- find 查找文件时如果当前工作目录不对，会找不到测试文件，导致误认为测试文件不存在。
- 添加 CI 步骤只需要扩展现有工作流，不需要新建文件，增量扩展最安全。

## 本次是否熵注入

- 本次迭代 113 不是 3 的倍数，未触发熵注入。
