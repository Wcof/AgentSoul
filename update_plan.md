# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- Web UI 记忆列表性能优化已完成：默认只展开最新年月分组，收起分组采用懒渲染只在点击展开时才创建DOM节点。大幅减少初始DOM节点数，显著提升大型记忆库滚动性能。
- 安装回滚机制完整闭环已经完成：自动回滚处理安装失败/中断，手动回滚允许用户主动撤销安装。现在安装过程已经完全原子化：要么成功完成，要么完全回滚到安装前。
- 当前所有功能都已闭环，回滚机制完整。
- 下一个最值得继续放大的最小闭环：检查并清理重复的 JSON schema 验证代码，继续精简统一健康闸门体系。

## 本轮新增功能的延续方向

- Web UI 记忆列表性能优化已完整实现：
  - 默认只展开最新（第一个）年月分组，其他分组默认收起
  - 收起分组不渲染DOM节点，只有点击展开后才延迟渲染记忆项
  - 大幅减少初始页面加载时的DOM节点数量
  - 显著改善加载数十个记忆文件后的滚动性能，提升大型记忆库使用体验
- 用户现在打开Web UI时不会因为大量DOM节点导致卡顿，即使积累了数年的记忆文件依然能保持流畅。

## 本轮精简链路的继续优化方向

- 本次优化只修改了渲染逻辑，没有新增重复代码，两个 `renderMemoryList` 函数（原始版本和标签筛选版本）都同步更新，保持行为一致性
- 继续保持每次迭代一次小精简，保持代码干净。

## 本轮执行方式迭代的延续方向

- 本次迭代完成了Web UI性能优化，从"全量渲染所有分组所有记忆"到"默认只渲染最新分组点击展开懒渲染"，用最小代码改动获得最大性能收益
- 继续坚持"渐进式优化"原则，在现有架构基础上小步改进，不搞大规模重构。

## 必做项（本次遗留 + 卡死项追踪）

- 保持所有测试通过，健康度满分。
- 暂无遗留卡死项。

## 二选一战略方向（下次按健康度择一）

下次迭代健康度 100，137 不整除 3 不触发熵注入，正常选择偏稳定：

- **A 偏稳定**：选择已存在功能做小步深化，检查并清理重复的 JSON schema 验证代码
- **B 偏破局**：若触发熵注入，可以探索记忆文件批量去重清理功能

## 功能候选 3 项

1. **（历史功能深化）** ✓ Web UI 记忆列表性能优化：当加载大量记忆文件时，实现虚拟化只渲染可见项 → 改善加载数十个记忆文件后的滚动性能，提升大型记忆库使用体验 **已完成**
2. **（历史功能深化）** 检查并清理重复的 JSON schema 验证代码 → 继续精简统一健康闸门体系，确保所有检查器使用一致的验证逻辑
3. **（自动升级探索）** 为 `entry_detect.py` 添加 URL 分享检测，识别当前环境是否在分享链接会话中，自动加载对应搜索条件 → 提升 URL 分享功能用户体验

## 本次踩坑与陷阱警告

- ruff 会提示未使用导入对于仅用于副作用的导入（如 `readline`），需要手动添加 `# noqa: F401` 禁止警告。
- 当在全局作用域已经导入 `yaml`，内部函数中重复导入会导致 ruff 错误"referenced before assignment"，即使代码运行正确，最好还是移除重复导入保持 ruff 干净。
- 实验分支合并后记得删除分支，保持分支列表干净。
- 添加新功能/新命令到 argparse 时，注意不要重复定义 main() 函数，会导致语法错误。编辑前确认文件结构避免重复定义。
- 增量扩展功能可能会产生函数副本（如 `renderMemoryList` 被原版本和标签筛选扩展版本各保存一份），修改功能时必须同时更新两个副本保持行为一致。

## 本次为正常 → 结论

- Web UI 记忆列表性能优化实现完成，通过默认收起旧分组 + 懒渲染大幅减少初始DOM节点数，显著提升加载大量记忆文件后的滚动性能。该优化对大型记忆库使用体验提升明显，属于有用改进，可以保留。
