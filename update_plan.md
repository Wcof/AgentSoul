# 给下次迭代的战略信

## 当前阶段评估与最薄弱环节

- 当前健康度保持 **100**，全量测试 **638/638** 通过。
- entry_detect.py 入口探测注入路径现在完整闭环：环境检测 → 输出可用注入方法 → 生成可直接复制的注入模板 → 预演检查（检测现有配置文件冲突） → 实际注入，整个用户路径完整，用户在每个步骤都能得到明确指引，使用门槛进一步降低。
- Web UI 灵魂成长曲线功能已经完整闭环：导入 `.soul_health.md` → 绘制健康度曲线图 → 显示五项核心统计 → 自动生成趋势评估 → 导出文字/图片分享，整个用户流程通畅。
- MCP 订阅推送机制完整闭环：创建订阅 → 推送事件 → 指数退避重试 → 失败计数 → 超过次数自动退订 → 列出完整订阅状态，整个生命周期管理完成。
- 统一健康闸门体系已经完整闭环：JSON Schema 定义 → 三个 CLI 检查器输出标准格式 → GitHub Actions CI 闸门 → Web UI 两种检查报告导入可视化 → 所有文档同步更新。
- 两个核心 CI 工作流都已添加 `mypy --strict` 和 `ruff check` 静态检查，代码质量自动化验证完整。
- `.soul_health.md` 所有记录都已格式标准化，括号全部匹配，满足自动化读取要求。
- `.phase_summary.md` 所有阶段记录格式也已标准化，阶段性自审可以直接读取数据做趋势分析。
- `.capability_map.md` 已更新，技术债描述准确反映当前实际情况。
- `CLAUDE.md` 已经完整反映最新项目架构和功能，不需要更新。
- 检查确认代码干净，没有明显的重复调用、重复分支或重复输出路径需要精简。
- 当前所有功能都已闭环，没有明显薄弱环节，最值得继续放大的最小闭环：陪伴连续性检查器补齐统一 JSON 输出格式，支持 Web UI 可视化；最值得继续精简的最小链路：确认 TypeScript 构建输出没有冗余，保持 MCP 服务器构建简洁。

## 本轮新增功能的延续方向

- 延续"入口探测自动升级"成果，entry_detect 从简单的环境识别进化到完整注入流程支持：检测环境 → 生成模板 → 预演检查，用户只需要按步骤执行就能完成注入，降低了新用户入门门槛。
- 延续"统一健康闸门"体系优势，新增功能直接复用标准 HealthSummary JSON 格式，不需要定义新格式，Web UI 可以直接消费展示，减少了格式适配成本。

## 本轮精简链路的继续优化方向

- 已经检查确认上轮 Web UI 导出功能新增代码干净，没有明显重复代码需要精简。
- 如果未来发现重复代码，可以继续收敛合并减少分叉。

## 本轮执行方式迭代的延续方向

- 继续强化"自动升级探索"方向，持续完善注入流程中用户体验，减少注入失败风险。
- 继续坚持"小步增量"，每次只处理一个小功能点，保持改动可控，测试通过率持续 100%。
- 继续坚持"所有核心流程都要有 CI 验证"，减少人工失误，保证质量一致。
- 继续坚持"阶段性自审每 5 轮一次"，用数据驱动战略调整，保持项目方向正确。

## 必做项（本次遗留 + 卡死项追踪

- 保持统一健康体系闭环可用，不要让后续配套文档与实际能力脱节。
- 暂无遗留卡死项。

## 二选一战略方向（下次按健康度择一）

下次迭代是 128，不是 3 的倍数也不是 5 的倍数，按实际健康度和熵检测结果选择：
- A 偏稳定：选择一个已存在功能做小步深化，保持健康度满分
- B 偏破局：若触发熵注入，选择一个适中规模的新探索方向

## 功能候选 3 项

1. **（历史功能深化）** 陪伴连续性检查器补齐：将结果也输出统一 HealthSummary JSON 格式，支持 Web UI 可视化 → 延续统一健康闸门体系，让陪伴连续性检查也能在 Web UI 展示
2. **（历史功能深化）** 完善 entry_detect 增强：检测后根据预演结果自动生成 `starter-prompt.md` 完整启动提示文件 → 进一步降低新用户使用门槛
3. **（自动升级探索）** 探索记忆冲突自动检测：检测现有记忆与注入目标之间的内容冲突，给出合并建议 → 执行方式自我迭代，提升注入可靠性

## 本次踩坑与陷阱警告

- 添加新 CLI 选项后，帮助信息必须同步更新，保持文档与代码一致，否则用户找不到新选项。
- 新添加功能复用统一 HealthSummary 格式时，checker_name 需要明确区分，方便 Web UI 识别。

## 本次是否熵注入

- 本次迭代 127 不触发熵注入。
