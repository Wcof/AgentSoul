# 给下次迭代的战略信

## 当前阶段评估

- 当前健康度 **100**，处于极佳稳定状态
- 迭代 92（第 19 阶段阶段性自审完成），连续 89 轮迭代健康度保持 100
- 所有 577 个测试 100% 通过，总体覆盖率 88%，entry_detect 覆盖率从 45% → 75%
- MCP 编译成功，ruff 检查零警告，mypy --strict 零错误通过
- 完成本次功能深化：
  - 修复 mypy 缺少返回类型注解错误
  - 修复 ruff 检测出的代码风格问题（import 排序、过时 typing 语法）
  - 将旧的 `typing.List/Dict/Tuple` 更新为现代 `list/dict/tuple` 语法
  - 新增 2 个单元测试，entry_detect 覆盖率提升 30 个百分点
  - 完成第 19 阶段阶段性自审，近 5 轮健康度均值 100，趋势平稳满分
  - 创建 git tag `phase-19-<timestamp>`
- 项目持续保持完全稳定可发布状态

## 必做项

- 无遗留卡死项
- 保持项目健康度 100
- 等待 GitHub CI 运行验证本次更新（静态检查修复 + 测试覆盖率提升）
- 验证 MCP 订阅推送机制完整功能端到端链路仍然工作正常

## 下次战略选择

### A 偏稳定
- 等待 GitHub CI 验证，若有问题则修复
- 持续监控依赖更新，及时补丁
- 保持健康度 100
- 如果 entry_detect 还有剩余覆盖率缺口可继续补充，但不是必须

### B 偏破局
- 验证 MCP 订阅推送机制完整功能端到端链路
- 可以尝试进一步提升低覆盖率模块的测试覆盖率
- 探索新的增值功能方向，例如 MCP 订阅推送 Webhook 测试

## 功能候选池

1. **验证 GitHub CI 结果并修复发现的任何问题**
2. **验证 MCP 订阅推送机制完整功能端到端链路**
3. **进一步检查是否有其他低覆盖率模块可以补充测试**

## 本次踩坑警告

- TypeScript 中 fs.accessSync 返回 void，不能用于 if 条件判断，必须用 try-catch
- 表达式索引类型需要显式类型断言，TypeScript 不允许 string 直接索引自定义类型
- 接口属性命名必须保持一致性（snake_case），否则 schema 验证和接口实现不匹配会导致编译错误
- 修改 TypeScript 代码后必须重新 npm run build，否则修改不会生效
- CSS 媒体查询嵌套时要小心多余开标签，会导致整个媒体查询规则解析失败
- 不同工具入口的规则承载点不同，不能假设 Claude Code、Codex、Gemini 都支持同一种文件或同一种 MCP 能力
- ruff 可以自动修复大部分导入排序、过时语法问题，只需运行 `ruff check src/ --fix`
- 不使用 bare except，应该明确捕获 `Exception` 满足 ruff 规则

## 阶段结论

本次迭代（阶段性自审 - 第 19 阶段）：
- 健康度保持 100 连续 89 轮
- 完成阶段性自审，近 5 轮均值健康度 100，趋势平稳满分
- 提升 entry_detect 测试覆盖率，修复所有静态检查错误
- 所有 577 个单元测试全部通过
- MCP 服务器编译成功
- mypy 严格模式零错误
- ruff 检查零警告
- 无 TODO/FIXME/HACK 遗留
- 项目持续保持完全稳定可发布状态
